diff --git a/home-assistant-voice.yaml b/home-assistant-voice.yaml
index 2bb3cc6..d0f379a 100644
--- a/home-assistant-voice.yaml
+++ b/home-assistant-voice.yaml
@@ -303,45 +303,53 @@ binary_sensor:
               then:
                 - if:
                     condition:
-                      switch.is_on: timer_ringing
+                      lambda: |-
+                        const auto state = id(joydex_voice_session_state).current_option();
+                        return state == "Listening" || state == "Muted";
                     then:
-                      - switch.turn_off: timer_ringing
+                      - script.execute: joydex_voice_hangup_pulse
                     else:
                       - if:
                           condition:
-                            voice_assistant.is_running:
+                            switch.is_on: timer_ringing
                           then:
-                            - voice_assistant.stop:
+                            - switch.turn_off: timer_ringing
                           else:
                             - if:
                                 condition:
-                                  media_player.is_announcing:
-                                    id: external_media_player
+                                  voice_assistant.is_running:
                                 then:
-                                  media_player.stop:
-                                    announcement: true
-                                    id: external_media_player
+                                  - voice_assistant.stop:
                                 else:
                                   - if:
                                       condition:
-                                        media_player.is_playing:
+                                        media_player.is_announcing:
                                           id: external_media_player
                                       then:
-                                        - media_player.pause:
-                                            id: external_media_player
+                                        media_player.stop:
+                                          announcement: true
+                                          id: external_media_player
                                       else:
                                         - if:
                                             condition:
-                                              and:
-                                                - switch.is_off: master_mute_switch
-                                                - not: voice_assistant.is_running
+                                              media_player.is_playing:
+                                                id: external_media_player
                                             then:
-                                              - script.execute:
-                                                  id: play_sound
-                                                  priority: true
-                                                  sound_file: "center_button_press_sound"
-                                              - delay: 300ms
-                                              - script.execute: joydex_voice_wake_pulse
+                                              - media_player.pause:
+                                                  id: external_media_player
+                                            else:
+                                              - if:
+                                                  condition:
+                                                    and:
+                                                      - switch.is_off: master_mute_switch
+                                                      - not: voice_assistant.is_running
+                                                  then:
+                                                    - script.execute:
+                                                        id: play_sound
+                                                        priority: true
+                                                        sound_file: "center_button_press_sound"
+                                                    - delay: 300ms
+                                                    - script.execute: joydex_voice_wake_pulse
       # Double Click
       #  . Exposed as an event entity. To be used in automations inside Home Assistant
       - timing:
@@ -391,14 +399,22 @@ binary_sensor:
               condition:
                 lambda: return !id(init_in_progress) && !id(color_changed) && !id(group_volume_changed);
               then:
-                - script.execute:
-                    id: play_sound
-                    priority: false
-                    sound_file: "center_button_long_press_sound"
-                - light.turn_off: voice_assistant_leds
-                - event.trigger:
-                    id: button_press_event
-                    event_type: "long_press"
+                - if:
+                    condition:
+                      lambda: |-
+                        const auto state = id(joydex_voice_session_state).current_option();
+                        return state == "Listening" || state == "Muted";
+                    then:
+                      - script.execute: joydex_voice_toggle_mute_pulse
+                    else:
+                      - script.execute:
+                          id: play_sound
+                          priority: false
+                          sound_file: "center_button_long_press_sound"
+                      - light.turn_off: voice_assistant_leds
+                      - event.trigger:
+                          id: button_press_event
+                          event_type: "long_press"
       # Very important do not remove. Trust me :D
       - timing:
           # H ....
@@ -1054,6 +1070,8 @@ script:
             id(control_leds_dial_touched).execute();
           } else if (id(timer_ringing).state) {
             id(control_leds_timer_ringing).execute();
+          } else if (id(joydex_voice_session_state).current_option() == "Muted") {
+            id(control_leds_joydex_voice_muted).execute();
           } else if (id(voice_assistant_phase) == ${voice_assist_waiting_for_command_phase_id}) {
             id(control_leds_voice_assistant_waiting_for_command_phase).execute();
           } else if (id(voice_assistant_phase) == ${voice_assist_listening_for_command_phase_id}) {
